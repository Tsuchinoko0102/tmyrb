require 'rails_helper'

RSpec.describe Book, type: :model do
  describe "Bookモデル登録のテスト" do
    # 正常系
    it "すべての項目が入力されていたら登録できる" do

    end

    # 異常系
    it "titleが空欄だと登録できない" do

    end

    it "authorが空欄だと登録できない" do

    end

    it "genre_idが空欄だと登録できない" do

    end

    it "genre_idが1の場合は登録できない" do

    end

    it "leadが空欄だと登録できない" do

    end

    it "contentが空欄だと登録できない" do

    end

    it "rank_idが空欄だと登録できない" do

    end

    it "rank_idが1の場合は登録できない" do

    end

    it "publishが空欄だと登録できない" do

    end

    it "userが空欄だと登録できない" do

    end

    it "imageが空欄だと登録できない" do

    end

    it "urlが空欄だと登録できない" do
      
    end
  end
end
